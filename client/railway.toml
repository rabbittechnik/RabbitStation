# Railway WORKDIR = Monorepo-Root (/app). Kein "cd .." — das wechselt nach "/" und scheitert.
[build]
builder = "railpack"
buildCommand = "npm run build"

[deploy]
startCommand = "node client/scripts/start-production.mjs"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
